/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "placehold.co",
      "via.placeholder.com",
      "ik.imagekit.io",
      "picsum.photos",
    ],
  },
};

module.exports = nextConfig;
